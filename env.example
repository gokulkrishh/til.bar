# Supabase
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
NEXT_PUBLIC_SUPABASE_URL=""
SUPABASE_PROJECT_ID=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_JWT_SECRET="" # Supabase Dashboard → Settings → API → JWT Secret

# OpenRouter
OPENROUTER_API_KEY=""

# Email (Resend) — weekly digest
RESEND_API_KEY=""
EMAIL_FROM="til.bar <no-reply@til.bar>"

# Cron — weekly digest auth
CRON_SECRET=""

# Weekly digest — max users that receive AI-synthesized email per run (others skipped, no fallback)
WEEKLY_DIGEST_AI_MAX_USERS="10"